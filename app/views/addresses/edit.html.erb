<h1>Editing address</h1>
<%= render "form", address: @address %>
<br>
<div>
  <%= link_to "Show this address", new_client_address_path(@client,@address), class:"btn btn-info" %> ||
  <%= link_to "Back to addresses", client_addresses_path(@client,@address), class:"btn btn-primary" %>
</div>
