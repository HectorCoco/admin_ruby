<%= form_with(model: phone) do |form| %>
  <% if phone.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(phone.errors.count, "error") %> prohibited this phone from being saved:</h2>
      <ul>
        <% phone.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="mb-3">
    <%= form.label "Client", style: "display: block"%>
    <%= form.select :client_id, @clients.collect { |c| [ "#{c.first_name} #{c.middle_name} #{c.last_name}", c.id ] }, {},class:"" %>
  </div>
  <div class="mb-3">
    <%= form.label :number, style: "display: block" %>
    <%= form.text_field :number %>
  </div>
  <br>
  <div class="mb-3">
    <%= form.submit %>
  </div>
<% end %>
