
    document.addEventListener("load", cargaPagina);
    function change_amount() {
    document.getElementById("batch").addEventListener("click", change_amount);
    }
    function change_amount() {
    var value = document.getElementsById("amount");
    value.value = "hola mundo"
    }
